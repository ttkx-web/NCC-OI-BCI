name: bci-dayloop
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy=1.26.4
  - scipy>=1.11
  - h5py>=3.10
  - pyyaml>=6.0
  - mne>=1.6
  - scikit-learn>=1.4
  - pyedflib>=0.1.36
  - pip

# This is the portable, CPU-neutral base environment. Install the approved
# CUDA-specific PyTorch wheel separately after activation; do not pin a CUDA
# toolkit or wheel index in this file. See docs/server_deployment.md.
