# Portable runtime dependencies. Install the platform-approved GPU torch wheel
# separately; requirements files never choose a CUDA wheel index.
numpy==1.26.4
scipy>=1.11
h5py>=3.10
PyYAML>=6.0
torch>=2.5
mne>=1.6
scikit-learn>=1.4
pyedflib>=0.1.36
